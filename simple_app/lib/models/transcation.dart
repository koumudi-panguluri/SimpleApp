class Transaction {
  final String title;
  final int id;
  final double price;
  final String currency;
  final DateTime date;
  final int color;
  Transaction(
      {this.title, this.id, this.price, this.currency, this.date, this.color});
}
